---
title: Readmoo 看書桌面版电子书加密技术分析
date: 2026-09-10 20:00:00
permalink: /pages/4870a7/
sidebar: false
category:
  - 开发
tag:
  - 杂项
  - 逆向
  - DRM
---

## 前言

电子书DRM是一个具有争议性的话题，大概从法律意义上讲，用户对自己合法购买的电子书档案使用技术手段去除DRM这件事单纯来讲并没有违法，只有到了未获授权分发这一行为才可能构成侵犯著作权。[这里](https://www.cnblogs.com/yyfll/p/19496156)有一个关于Readmoo iOS版本的实现分析，但是好像和macOS当前版本客户端的行为不一致。网路上可以检索到的`demoo`等开源工具也均不适配当前版本macOS客户端。本文是对当前版本客户端行为进行的分析。出于明显原因，本文不涉及任何用于破解该平台DRM的工具代码。不过这个平台使用的DRM方案确实非常有意思。

## 整体架构速览

在进入加密细节之前，先建立一个大致的印象。Readmoo 看書桌面版是一个典型的 Electron 应用：

- **主进程**负责本地存储、密钥管理和解密服务；
- **渲染进程**里跑的是一个开源的 EPUB 阅读器引擎（foliate）；
- **书档**下载后直接以解压目录的形态落盘，阅读时按需取用；
- **密钥材料**统一放在一个本地加密配置库里。

真正有意思的地方在于：解密这件事被做成了一个**只监听本机的 HTTP 服务**，阅读器每次请求某个资源，主进程才临时解密并返回。这套结构会贯穿后面所有的讨论。

## 内容层：沿用标准加密格式

书的 `META-INF/encryption.xml` 遵循的是 OCF / XML Encryption 那套行业标准，和 Adobe ADEPT、Readium 的内容加密同源。它的结构可以概括为：

- 每个受保护的文件对应一条 `EncryptedData` 记录；
- 加密算法标注为 `aes256-cbc`；
- 密文文件本身是 `IV(16 字节) + ciphertext`，其中 IV 直接前置；
- 明文按 PKCS#7 补齐；
- 记录里还带一个 `Compression` 字段，当压缩方式不为 0 时，明文是先做 raw DEFLATE 再加密的。

换句话说，**内容层没有自研任何加密算法**。AES-256-CBC、前置 IV、PKCS#7、可选压缩，全是 EPUB 生态里成熟且常见的做法。这也意味着，如果只盯着 `encryption.xml` 看，会误以为这是一个标准方案——真正的门槛在密钥。

## `license.lcpl`：一个兼容性占位符

每条 `EncryptedData` 的 `KeyInfo` 里，都写着一个看起来非常"标准 LCP"的引用：

```
license.lcpl#/encryption/content_key
```

标准的 Readium LCP 里，`.lcpl` 是一个真实的授权文件，里面装着被用户口令派生密钥加密后的 content key；阅读器拿到它之后，配合用户输入的 passphrase 才能解出真正的密钥。

但 Readmoo 这里并不是这样：

- 书档目录里并没有一个承担实际作用的授权文件；
- 阅读器也不会去解析什么 `license.lcpl`；
- 真正的 content key 另有去处。

所以这行引用更像是**借用了 LCP 的 `RetrievalMethod` 写法来占位**，让人以为这是标准 LCP，实际上密钥管理完全是自研的。这种设计的巧妙之处在于：它让格式在结构上保持对标准阅读器的兼容，同时把真正的控制权收回到了自己的实现里。

## 密钥分发：设备绑定的信封加密

真正的内容密钥是一把对称密钥（每本书一把），而它的分发走的是经典的**信封加密 / 混合加密**思路：

1. 设备端在登录时生成一对非对称密钥，公钥登记到服务端，私钥留在本机；
2. 服务端在授权/下载时，用这把设备公钥把每本书的对称 content key 加密后下发；
3. 设备端把密文保存下来，阅读时再用本地私钥解出 content key。

用一张图概括就是：

```
服务端                                    设备端
  content_key ──► RSA(设备公钥) ──►  密文 ──► 本地储存
                                              │
                                   阅读时 RSA(设备私钥) 解出
                                              ▼
                                        content_key
```

这套设计有几个很典型的特征：

- **离线可读**：content key 已经预置到本地，阅读时不需要联网向授权服务器换取密钥；
- **设备绑定**：密钥是对着某一台设备的公钥封装的，换机器就解不开；
- **按登录会话轮换**：每次登录会带一个时间戳，密钥记录与该时间戳绑定，形成一种轻量的密钥轮换。

代价也很明显：既然是"预置到本地"，那密钥材料就必然存在于设备上，剩下能做的就是**怎么把它藏得更好**——这正是下一节的主题。

## 密钥派生与储存：五层保护

从设备标识到最终的文件明文，中间其实叠了好几层保护。把它们摊开来看，层次感相当清晰：

| 对象 | 保护方式 | 作用 |
| --- | --- | --- |
| 机器身份 | 由系统硬件 UUID 派生 | 把整条链绑到这台机器 |
| 配置库密钥 | 机器身份 + 固定盐做哈希 | 解锁本地加密配置库 |
| 配置库本体 | 对称加密 + 口令派生函数 | 存放私钥与 content key 密文 |
| 非对称私钥 | 字符替换混淆 + 操作系统密钥链 | 解开 content key |
| content key | 非对称加密，可再套一层密钥链 | 解密书档 |

具体来说：

**第一层，机器身份。** 应用会读取系统的硬件唯一标识，做一次哈希后作为"这台机器"的代表。这一步是整个绑定关系的根。

**第二层，配置库密钥。** 在机器身份上再拼一个固定盐值做哈希，得到配置库的加密密钥。由于盐值固定，这一步本质上不是"加盐防爆破"，而是"把密钥绑定到机器"。

**第三层，配置库本体。** 本地配置库并不是明文 JSON，而是一个对称加密过的文件：密钥由口令派生函数（PBKDF2 + SHA-512）导出，密文结构是"IV + 分隔符 + 密文"的经典写法。RSA 私钥和所有 content key 的密文都躺在这里面。

**第四层，私钥的额外保护。** 非对称私钥在写入前会先做一次**自定义字符替换混淆**，再交给操作系统的密钥链（macOS 上是 Keychain，Electron 通过 `safeStorage` 抽象）。混淆表还带版本号，说明设计者预期未来会更换映射规则。私钥的明文本体则始终不落盘。

**第五层，content key 的可选保护。** content key 的密文同样可以被 `safeStorage` 再包一层。是否启用取决于平台能力。

把这条链画出来，就是：

```
系统硬件 UUID
      │ 哈希
      ▼
   机器身份 ──► 配置库密钥 ──► 解密配置库
                                   │
                    ┌──────────────┴──────────────┐
                    ▼                             ▼
              混淆 + 密钥链保护的私钥        RSA 密文形式的 content key
                    │                             │
                    └────────► 解出 ◄─────────────┘
                                 ▼
                            content key
                                 ▼
                            解密书档
```

这套设计的优点是把"防御"分散到了多个层次：想拿到 content key，得先拿到配置库密钥，而配置库密钥绑在机器上；就算拿到配置库，私钥还被密钥链保护着。但它也有一个根本性的天花板——**所有材料都在本地，没有任何一步需要服务端在线参与**。这意味着它提高的是"门槛"，而不是"可能性"。

## 阅读器实现：一个本地解密服务

密钥藏得再好，最终也要被阅读器用掉。阅读器的实现方式值得注意：它没有在打开书的时候把整本书解密到磁盘，而是**在 Electron 主进程里起了一个只监听本机的小型 HTTP 服务**，把"解密"变成一次 HTTP 请求。

整体流程大致是：

1. 阅读器（基于开源的 foliate 引擎）向本机服务请求某个书内资源；
2. 主进程根据请求路径定位到书档文件；
3. 查该书的 `encryption.xml`，确认这个文件是否受保护；
4. 取出 content key，做对称解密，必要时再解压；
5. 把明文结果作为 HTTP 响应返回。

其中有两个细节值得注意：

**其一，`encryption.xml` 会被主动隐藏。** 服务对这个文件直接返回 404。原因是标准阅读器（如 Readium）一旦看到 `encryption.xml`，就会倾向于把所有加密资源都预载进来做处理，其中包括很多当前页面根本用不到的字型档，开书速度会被拖慢。主动隐藏它，既避免了这些不必要的预载，也缩短了开书时间——反正解密逻辑本来就由主进程自己负责。

**其二，CSS 会被改写一遍。** 返回样式表时，服务会把里面引用的若干系统字体名替换成书籍内嵌的字体。这通常是为了排版一致性，也可能涉及字体授权问题，属于阅读体验层面的处理，与加密无关，但可以看出这个本地服务承担的职责远不止解密。

书档的落盘结构也印证了"下载即可用"的思路：

```
download/
├── library-items/<bookId>/     # 一般书籍
├── media-items/<bookId>/       # 杂志/多媒体
├── favor-items/<bookId>/       # 收藏
└── document-items/<bookId>/    # 文档
```

每个目录本身就是一本解压好的 EPUB，包含 `META-INF`、`OEBPS`、以及厂商自定义的扩展目录。阅读器不需要先解压，直接在目录上按需取用即可。

## 安全评估

把上面几块拼起来，可以给出一个相对完整的评价。

**做得规范的地方：**

- 内容层完全采用行业标准，没有发明轮子，兼容性和可维护性都好；
- 密钥管理用了教科书式的信封加密，公私钥职责分明；
- 密钥储存分层清晰，复用了操作系统提供的密钥链能力，而不是自己造一个"加密算法"。

**值得商榷的地方：**

- **非对称密钥长度偏短。** 设备密钥的位数只有 512 位，且用的是较老的 PKCS#1 v1.5 填充。放在今天，这个强度已经远低于安全预期。当然，它保护的对象只是一把 32 字节的对称密钥，实际风险取决于攻击者的能力模型。
- **对称加密缺乏完整性保护。** AES-CBC 没有搭配 HMAC，也没有换成 AEAD 模式（如 GCM），因此密文在理论上具有可延展性。不过由于最终产物是 EPUB 这类有强结构约束的格式，篡改通常会导致解析失败而非静默的错误明文。
- **"设备绑定"是可用性与安全的经典取舍。** 预置密钥换来了离线阅读体验，但代价是密钥必然落到设备上。所谓 DRM 强度，最终取决于 512 位 RSA 和本地密钥保护，而不是 AES-256。

### 关于 "security by obfuscation" 的反思

私钥那层字符替换混淆，是一个相当典型的"通过隐匿实现安全"（security by obfuscation）样本。它不提供任何密码学意义上的强度：映射关系是双向可逆的，甚至因为带上了版本号而变得"自文档化"——看到前缀就知道该用哪张表还原。它真正能挡住的，只是随手做几次字符串搜索的临时翻找者；面对愿意读实现的人，它几乎不构成障碍。

需要说明的是，混淆本身并不是"错误的工程实践"。作为纵深防御的一环，它确实能抬高自动化批量提取的成本，也能让密钥材料不以 PEM 这种一眼可辨的形态出现。问题出在**把它当成安全边界**：Kerckhoffs 原则早已指出，安全性应当建立在密钥之上，而不是算法或实现细节的保密之上。一旦实现被公开，依赖隐匿的那一层就会立刻归零。

同样的性质也出现在配置库密钥的推导上：机器身份拼接固定盐值再哈希，看起来像"加盐"，但它既不防爆破（盐固定、空间有限），也不引入任何秘密，本质上是把密钥**绑定**到机器，而非**保护**它。真正承担安全职责的，其实只有操作系统的密钥链，以及那把（偏短的）非对称密钥。

把这几层叠起来看，会觉得防御相当"厚"；但逐层拆开就会发现，每一层都落在本地、都可以被同一台机器上的代码路径还原。这套方案用密码学强度换取了离线分发的便利，对阅读器而言是合理的工程取舍——只是它应当被诚实地描述为"抬高门槛"，而不是"守住秘密"。

**和主流方案的对比：** FairPlay、Adobe ADEPT、以及真正的 Readium LCP 都把密钥获取与在线授权服务器、用户口令或平台安全硬件深度绑定；Readmoo 这套更像是**自研的轻量离线方案**——结构上向标准靠拢，实现上自给自足。

## 结语

整体看下来，Readmoo 看書桌面版的这套方案其实相当规范：内容层用标准，密钥层用信封加密，储存层借用操作系统能力。真正自研的，只有 **key 的分发与保管**，以及**阅读器那个按需解密的本地服务**。

至于 `license.lcpl` 那行引用，不妨把它看作一个兼容性占位符：它保证了格式对标准阅读器的兼容，而真正的密钥控制权则被留在实现内部。
